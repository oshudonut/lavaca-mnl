// Stub — Google Calendar sync implemented in Phase 9.
// Called non-blocking from EVT-003 confirm route.
export async function syncOrderToCalendar(_orderId: string): Promise<void> {
  // Phase 9: will call lib/calendar/service.ts createOrderEvent
}
